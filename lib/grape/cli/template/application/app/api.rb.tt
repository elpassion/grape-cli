module <%= config[:app_name].split('_').collect(&:capitalize).join %>
  class API < Grape::API
    prefix 'api'
    format :json

    mount <%= config[:app_name].split('_').collect(&:capitalize).join %>::HelloWorld

    add_swagger_documentation api_version: 'v1'
  end
end
